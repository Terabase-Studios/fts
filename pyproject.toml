[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fts"
version = "0.1.0"
description = "Fake Tool Suite CLI"
authors = [
  { name = "Your Name", email = "you@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"

# <-- Add dependencies here
dependencies = [
    "cryptography>=41.0",  # replace with the version you want
    # add any other packages your project needs
]

[project.scripts]
fts = "fts.cli:main"
